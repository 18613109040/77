<!-- <view wx:if="{{showTost}}">
  <picker-view indicator-style="height: 50px;"  style="width: 100%; height: 300px;" value="{{currentSelect}}" bindchange="bindChange">
    <picker-view-column>
      <view wx:for="{{deliveryTime.timeSolts}}" class='picker-time' style="line-height: 50px">
        <view class='picker-view'>今天 </view>
        <view class='picker-view'> {{item.timeSolt}}</view>
        <view class='picker-view'>配送费 ¥{{deliveryTime.deliveryFee}}</view>
        </view>
    </picker-view-column>
  </picker-view>
</view> -->


 <view class="section">
  <picker bindchange="bindPickerChange" value="{{index}}" range-key="timeSolt" range="{{deliveryTime.timeSolts}}">
    <view class="picker">
      当前选择：{{array[index]}}
    </view>
  </picker>
</view> 